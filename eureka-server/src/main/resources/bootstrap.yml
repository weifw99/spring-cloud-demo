spring:
  application:
    name: eureka-server #指定微服务的名称后续在调用的时候只需要使用该名称就可以进行服务的访问
  cloud:
    config:
      enabled: false #禁用spring-cloud config
#      uri: http://localhost:8888
#      label: develop
